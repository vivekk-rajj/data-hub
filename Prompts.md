# Prompts Log (Sprint 10 Track B)

## AI Assistance Used

1. Debugging support for converting in-memory CRUD logic to MongoDB Atlas + Mongoose CRUD operations.
2. Explanation support for async error handling and HTTP response behavior (`400`, `404`, `500`) for Express routes.
3. Guidance for environment setup using `dotenv`, `.env.example`, and deployment env vars on Render.

## Human Review

- Reviewed all generated/assisted changes before finalizing.
- Verified no credentials or secrets were added to repository files.
- Confirmed `.env` remains ignored and only placeholders exist in `.env.example`.
